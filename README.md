> Based on on the book [Writing an Interpreter in Go](https://interpreterbook.com) by Thorsten Ball


Run tests with `pytest .`

For specific test case `pytest -q -s test_parser.py::test_let_statements`